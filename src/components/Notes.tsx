import styled from "@emotion/styled";

const NotesStyled = styled.div`
  color: red;
  font-size: 28px;
`;

function Notes() {
  return <NotesStyled>Notes here</NotesStyled>;
}

export default Notes;
